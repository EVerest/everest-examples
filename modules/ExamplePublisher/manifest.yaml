description: Simple example publisher module written in C++
config:
  voltage:
    description: The voltage the physical connector can supply
    type: number
    minimum: 1
    maximum: 60
  enum_test:
    description: A config value that tests the enum type
    type: string
    enum:
      - one
      - two
      - three
provides:
  example_pub:
    interface: example_publisher
    description: This implements an example interface that uses multiple framework features
enable_external_mqtt: true
metadata:
  license: https://opensource.org/licenses/Apache-2.0
  authors:
    - Kai-Uwe Hermann
